apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-gin-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: myapp.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-gin
                port:
                  number: 80
